# 🚀 البدء السريع - Firebase Backend

## ⚡ تشغيل المشروع في 3 خطوات

### الخطوة 1: إعداد Firebase (مرة واحدة فقط)

1. **إنشاء مشروع Firebase**
   - اذهب إلى: https://console.firebase.google.com/
   - انقر "Add project"
   - اسم المشروع: `philosopher-platform`

2. **تفعيل الخدمات**
   - **Authentication**: فعّل Email/Password و Google Sign-In
   - **Firestore Database**: أنشئ قاعدة بيانات (Test mode)
   - **Realtime Database**: أنشئ قاعدة بيانات (Test mode)

3. **الحصول على التكوين**
   - Project Settings ⚙️ → Your apps → Web app
   - انسخ `firebaseConfig`

### الخطوة 2: إعداد المشروع المحلي

```bash
# 1. افتح مجلد firebase-backend
cd firebase-backend

# 2. انسخ ملف البيئة
copy .env.example .env

# 3. افتح .env وأضف معلومات Firebase
notepad .env
```

### الخطوة 3: تشغيل الخادم

**الطريقة الأولى (الأسهل)**:
```
انقر مرتين على: RUN_FIREBASE_BACKEND.bat
```

**الطريقة الثانية**:
```bash
cd firebase-backend
npm install
npm start
```

---

## 🌐 الوصول للخادم

بعد التشغيل، افتح المتصفح على:

- **الصفحة الرئيسية**: http://localhost:3000
- **حالة الخادم**: http://localhost:3000/api/status
- **التوثيق الكامل**: http://localhost:3000/api/docs

---

## 📝 مثال سريع للاستخدام

### تسجيل مستخدم جديد

```javascript
import { registerUser } from './src/services/auth.service.js';

// تسجيل مدرس
const teacher = await registerUser(
  'teacher@example.com',
  'password123',
  'محمد ناصر الفيلسوف',
  'teacher'
);

// تسجيل طالب
const student = await registerUser(
  'student@example.com',
  'password123',
  'أحمد محمد',
  'student'
);
```

### إنشاء دورة جديدة

```javascript
import { createCourse } from './src/services/firestore.service.js';

const courseId = await createCourse({
  title: 'مقدمة في الفلسفة',
  description: 'دورة شاملة في أساسيات الفلسفة',
  instructorId: teacher.uid,
  thumbnailUrl: 'https://example.com/thumbnail.jpg'
});
```

### إضافة درس

```javascript
import { createLesson } from './src/services/firestore.service.js';

const lessonId = await createLesson({
  courseId: courseId,
  title: 'الدرس الأول: ما هي الفلسفة؟',
  videoUrl: 'https://youtube.com/watch?v=example',
  notes: 'ملاحظات الدرس',
  createdBy: teacher.uid
});
```

### إضافة ملاحظة مدرس (تُنشأ تلقائياً)

```javascript
import { createTeacherNote } from './src/services/firestore.service.js';
import { sendNotification } from './src/services/realtime.service.js';

// إنشاء الملاحظة
const noteId = await createTeacherNote({
  userId: teacher.uid,
  courseId: courseId,
  content: 'ملاحظة مهمة: يرجى مراجعة الدرس قبل الامتحان'
});

// إرسال إشعار للطلاب تلقائياً
await sendNotification({
  title: 'ملاحظة جديدة من المدرس',
  courseId: courseId,
  message: 'أضاف المدرس ملاحظة جديدة',
  type: 'info'
});
```

### تتبع تقدم الطالب

```javascript
import { markLessonCompleted } from './src/services/realtime.service.js';

// عند إكمال الطالب للدرس
await markLessonCompleted(student.uid, courseId, lessonId);
```

---

## 📊 هيكل قاعدة البيانات

### Firestore Collections (البيانات المنظمة)

```
users/          → ملفات المستخدمين
courses/        → الدورات
lessons/        → الدروس (روابط فيديو خارجية)
assignments/    → الواجبات
exams/          → الامتحانات
notes/          → ملاحظات المدرس (تُنشأ تلقائياً)
feedback/       → تقييمات الطلاب
```

### Realtime Database (التحديثات الفورية)

```
progress/       → تقدم الطلاب
notifications/  → الإشعارات الفورية
liveChat/       → الدردشة المباشرة
```

---

## 🔐 الأمان

### الصلاحيات التلقائية:

✅ **الطلاب**:
- قراءة الدورات والدروس
- إضافة تقييمات
- تتبع التقدم الخاص بهم

✅ **المدرسون**:
- إنشاء وتعديل الدورات
- إضافة دروس وامتحانات
- إنشاء ملاحظات (تظهر تلقائياً للطلاب)
- إرسال إشعارات

---

## 🎯 الميزات الرئيسية

### ✅ المصادقة (Authentication)
- Email/Password
- Google Sign-In
- أدوار المستخدمين (student/teacher)
- إنشاء ملف تعريف تلقائي

### ✅ إدارة الدورات
- إنشاء دورات
- إضافة دروس مع روابط فيديو خارجية
- إنشاء امتحانات وواجبات

### ✅ ملاحظات المدرس
- تُنشأ تلقائياً عند الإضافة
- مرئية لجميع طلاب الدورة
- إشعار تلقائي للطلاب

### ✅ تتبع التقدم
- تحديث فوري لتقدم الطالب
- تتبع الدروس المكتملة
- تتبع الامتحانات المكتملة

### ✅ الإشعارات الفورية
- إشعارات real-time
- تصنيف حسب النوع (info, warning, success)
- ربط بالدورات

### ✅ الدردشة المباشرة
- دردشة لكل دورة
- تحديثات فورية
- تخزين الرسائل

---

## 📚 الملفات المهمة

| الملف | الوصف |
|-------|-------|
| `README.md` | نظرة عامة على المشروع |
| `SETUP_GUIDE.md` | دليل الإعداد التفصيلي |
| `ARCHITECTURE.md` | شرح البنية المعمارية |
| `QUICK_START_AR.md` | هذا الملف - البدء السريع |
| `src/test/testFunctions.js` | أمثلة الاستخدام |

---

## 🔗 روابط خارجية للفيديوهات

بدلاً من Firebase Storage (محدود في الخطة المجانية)، استخدم:

### YouTube
```javascript
videoUrl: 'https://youtube.com/watch?v=VIDEO_ID'
```

### Vimeo
```javascript
videoUrl: 'https://vimeo.com/VIDEO_ID'
```

### Google Drive
```javascript
videoUrl: 'https://drive.google.com/file/d/FILE_ID/view'
```

---

## 🌐 النشر على الإنترنت

### Vercel (الأسرع)
```bash
npm install -g vercel
vercel login
vercel --prod
```

### Railway
1. اذهب إلى railway.app
2. ربط مع GitHub
3. نشر المشروع

### Render
1. اذهب إلى render.com
2. إنشاء Web Service
3. ربط المستودع

---

## 🆘 حل المشاكل الشائعة

### ❌ Firebase not initialized
**الحل**: تأكد من ملء جميع المتغيرات في `.env`

### ❌ Permission denied
**الحل**: راجع قواعد الأمان في Firebase Console

### ❌ Module not found
**الحل**: قم بتشغيل `npm install` في مجلد firebase-backend

### ❌ Port already in use
**الحل**: غيّر PORT في `.env` إلى رقم آخر (مثل 3001)

---

## 💡 نصائح مهمة

1. **لا ترفع ملف `.env` إلى Git** - يحتوي على معلومات حساسة
2. **استخدم الروابط الخارجية للفيديوهات** - لتوفير مساحة Firebase
3. **راجع حدود الخطة المجانية** - 50K قراءة/يوم في Firestore
4. **أضف الفهرسة للاستعلامات المعقدة** - لتحسين الأداء
5. **استخدم Realtime DB للبيانات المتغيرة** - مثل التقدم والإشعارات

---

## ✅ الخلاصة

الآن لديك:
- ✅ Backend كامل باستخدام Firebase
- ✅ مصادقة بالبريد الإلكتروني و Google
- ✅ قاعدة بيانات منظمة (Firestore)
- ✅ تحديثات فورية (Realtime Database)
- ✅ قواعد أمان محكمة
- ✅ بنية نظيفة وقابلة للتوسع
- ✅ توثيق شامل

**🚀 جاهز للاستخدام والتطوير!**

---

للمزيد من التفاصيل، راجع:
- `SETUP_GUIDE.md` - دليل الإعداد الكامل
- `ARCHITECTURE.md` - شرح البنية المعمارية
- `src/test/testFunctions.js` - أمثلة عملية
