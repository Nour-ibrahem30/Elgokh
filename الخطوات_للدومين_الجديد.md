# 🌐 خطوات تفعيل Google Sign-In على الدومين الجديد

## ✅ الوضع الحالي
- ✅ تسجيل الدخول بـ Google يعمل على localhost
- ⏳ يجب تفعيله على الدومين الجديد

---

## 📋 الخطوات المطلوبة

### 1️⃣ معرفة الدومين الجديد

أولاً، تحتاج لمعرفة الدومين الجديد بالضبط. مثلاً:
- `example.com`
- `www.example.com`
- `mysite.netlify.app`
- `mysite.vercel.app`
- `a-platform-for-learning.web.app` (Firebase Hosting)

---

### 2️⃣ إضافة الدومين في Firebase Console

#### الخطوات:
1. اذهب إلى: https://console.firebase.google.com/
2. اختر مشروع **a-platform-for-learning**
3. من القائمة اليسرى: **Authentication** (🔐)
4. اضغط على تبويب **Settings** في الأعلى
5. انزل لقسم **Authorized domains**
6. اضغط **Add domain**
7. اكتب الدومين الجديد (بدون https:// أو http://)
   - مثال: `example.com` أو `www.example.com`
8. اضغط **Add**

#### يجب أن يكون عندك:
```
✅ localhost
✅ 127.0.0.1
✅ a-platform-for-learning.firebaseapp.com
✅ الدومين الجديد (مثلاً: example.com)
```

---

### 3️⃣ إضافة الدومين في Google Cloud Console

#### الخطوات:
1. اذهب إلى: https://console.cloud.google.com/
2. اختر نفس المشروع
3. من القائمة: **APIs & Services** > **Credentials**
4. ابحث عن **OAuth 2.0 Client IDs**
5. اضغط على اسم الـ Client (عادة: Web client)

#### أضف في Authorized JavaScript origins:
اضغط **+ ADD URI** وأضف:
```
https://الدومين-الجديد.com
```
مثال:
```
https://example.com
https://www.example.com
```

#### أضف في Authorized redirect URIs:
اضغط **+ ADD URI** وأضف:
```
https://الدومين-الجديد.com/__/auth/handler
```
مثال:
```
https://example.com/__/auth/handler
https://www.example.com/__/auth/handler
```

⚠️ **مهم جداً:** لا تنسى `/__/auth/handler` في النهاية!

7. اضغط **SAVE** في الأسفل

---

### 4️⃣ رفع الملفات على الدومين الجديد

إذا كنت تستخدم:

#### Firebase Hosting:
```bash
firebase deploy
```

#### Netlify:
- ارفع الملفات عبر Netlify Dashboard
- أو استخدم Netlify CLI

#### Vercel:
```bash
vercel --prod
```

#### استضافة عادية:
- ارفع جميع الملفات عبر FTP أو cPanel

---

### 5️⃣ اختبار على الدومين الجديد

1. افتح الموقع على الدومين الجديد
2. اذهب لصفحة تسجيل الدخول
3. اضغط على "تسجيل الدخول بـ Google"
4. يجب أن يعمل بدون مشاكل! ✅

---

## 🧪 اختبار متقدم

إذا أردت اختبار الدومين الجديد بشكل دقيق:

1. ارفع ملف `test-google-auth.html` على الدومين الجديد
2. افتحه في المتصفح: `https://yourdomain.com/test-google-auth.html`
3. اضغط على زر "اختبار تسجيل الدخول"
4. سيعطيك تشخيص دقيق

---

## ⏱️ ملاحظات مهمة

### الوقت:
- بعد إضافة الدومين في Firebase: فوري (ثواني)
- بعد إضافة الدومين في Google Cloud: قد يأخذ 5-10 دقائق

### الـ Cache:
- امسح الـ Cache بعد التحديثات
- أو جرب في Incognito Mode

### HTTPS:
- يجب أن يكون الدومين يعمل بـ HTTPS (وليس HTTP)
- Firebase Hosting يوفر HTTPS تلقائياً
- Netlify و Vercel يوفرون HTTPS تلقائياً

---

## 🐛 إذا ظهرت مشكلة

### خطأ: "redirect_uri_mismatch"
**الحل:** تأكد من إضافة `/__/auth/handler` في Google Cloud Console

### خطأ: "unauthorized-domain"
**الحل:** تأكد من إضافة الدومين في Firebase Console

### خطأ: "This app is blocked"
**الحل:** 
1. اذهب لـ Google Cloud Console
2. **OAuth consent screen**
3. غيّر من **Testing** إلى **In production**

---

## 📞 هل تحتاج مساعدة؟

إذا واجهت مشكلة:
1. أخبرني ما هو الدومين الجديد بالضبط
2. أرسل screenshot للخطأ
3. جرب ملف `test-google-auth.html` على الدومين الجديد

---

## ✅ قائمة التحقق النهائية

قبل الاختبار على الدومين الجديد:

- [ ] أضفت الدومين في Firebase Console (Authorized domains)
- [ ] أضفت الدومين في Google Cloud Console (JavaScript origins)
- [ ] أضفت الـ Redirect URI في Google Cloud Console
- [ ] رفعت الملفات على الدومين الجديد
- [ ] الدومين يعمل بـ HTTPS
- [ ] مسحت الـ Cache أو جربت في Incognito
- [ ] انتظرت 5-10 دقائق بعد التحديثات

---

**ملاحظة:** تسجيل الدخول يعمل على localhost، لذلك الكود صحيح 100%! فقط تحتاج لتحديث الإعدادات للدومين الجديد. 💪

**حظاً موفقاً! 🚀**
